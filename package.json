{
  "name": "p2p-exchange-pmr",
  "version": "1.0.0",
  "private": true,
  "description": "P2P платформа для обмена валют в Приднестровье",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\" \"npm run bot:dev\"",
    "server:dev": "cd server && npm run dev",
    "client:dev": "cd client && npm run dev",
    "bot:dev": "cd bot && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install && cd ../bot && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  },
  "keywords": [
    "p2p",
    "exchange",
    "telegram",
    "mini-app",
    "pmr"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}